local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local RarityColors = {
	common = Color3.fromRGB(0, 255, 0),
	uncommon = Color3.fromRGB(0, 170, 255),
	rare = Color3.fromRGB(170, 0, 255),
	epic = Color3.fromRGB(255, 200, 0),
	legendary = Color3.fromRGB(255, 0, 0),
	mythic = Color3.fromRGB(255, 0, 0),
	divine = Color3.fromRGB(255, 0, 0),
	secret = Color3.fromRGB(255, 0, 0)
}

local DefaultColor = Color3.fromRGB(255, 255, 255)

local espEnabled = false
local tracked = {}
local watchedFolders = {}
local connections = {}
local heartbeatConn = nil
local heartbeatAcc = 0
local translatedTexts = {}

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "PetESPGui"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = PlayerGui

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 130, 0, 40)
toggleBtn.Position = UDim2.new(0, 15, 0.5, -20)
toggleBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
toggleBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
toggleBtn.Text = "宠物透视: 关"
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 15
toggleBtn.Active = true
toggleBtn.Draggable = true
toggleBtn.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = toggleBtn

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Thickness = 1
stroke.Transparency = 0.6
stroke.Parent = toggleBtn

local function translateText(text)
	if not text or text == "" or #text < 2 then
		return nil
	end
	if translatedTexts[text] then
		return translatedTexts[text]
	end
	local success, result = pcall(function()
		local url = "https://translate.googleapis.com/translate_a/single?client=gtx&sl=auto&tl=zh-CN&dt=t&q=" .. HttpService:UrlEncode(text)
		local response = game:HttpGet(url)
		local decoded = HttpService:JSONDecode(response)
		if decoded and decoded[1] and decoded[1][1] and decoded[1][1][1] then
			return decoded[1][1][1]
		end
		return nil
	end)
	if success and result then
		translatedTexts[text] = result
		return result
	end
	return nil
end

local function isEnglish(text)
	if not text or text == "" then
		return false
	end
	local englishCount = 0
	local totalCount = 0
	for char in string.gmatch(text, ".") do
		local byte = string.byte(char)
		if byte then
			totalCount = totalCount + 1
			if (byte >= 65 and byte <= 90) or (byte >= 97 and byte <= 122) then
				englishCount = englishCount + 1
			end
		end
	end
	if totalCount == 0 then
		return false
	end
	return (englishCount / totalCount) > 0.5
end

local function getRarityColor(rarity)
	if not rarity then
		return DefaultColor
	end
	local key = string.lower(tostring(rarity))
	for name, color in pairs(RarityColors) do
		if string.find(key, name) then
			return color
		end
	end
	return DefaultColor
end

local function getInfoFolder()
	return PlayerGui:FindFirstChild("PetInfoBillboardUIs")
end

local function getPetInfo(model)
	local info = {}
	local folder = getInfoFolder()
	if folder then
		local bb = folder:FindFirstChild(model.Name)
		local frame = bb and bb:FindFirstChild("Frame")
		if frame then
			local petName = frame:FindFirstChild("PetName")
			if petName and petName:IsA("TextLabel") and petName.Text ~= "" then
				info.Name = petName.Text
			end
			local rarity = frame:FindFirstChild("Rarity")
			if rarity and rarity:IsA("TextLabel") then
				if rarity.Text ~= "" then
					info.Rarity = rarity.Text
				else
					for _, c in ipairs(rarity:GetChildren()) do
						if c:IsA("UIGradient") then
							info.Rarity = c.Name
							break
						end
					end
				end
			end
			local level = frame:FindFirstChild("Level")
			if level and level:IsA("TextLabel") and level.Text ~= "" then
				info.Level = level.Text
			end
		end
	end
	local attrs = model:GetAttributes()
	for k, v in pairs(attrs) do
		local lk = string.lower(k)
		if string.find(lk, "weight") then
			info.Weight = tostring(v)
		elseif string.find(lk, "age") then
			info.Age = tostring(v)
		end
	end
	for _, child in ipairs(model:GetChildren()) do
		if child:IsA("NumberValue") or child:IsA("IntValue") or child:IsA("StringValue") then
			local lk = string.lower(child.Name)
			if string.find(lk, "weight") and not info.Weight then
				info.Weight = tostring(child.Value)
			elseif string.find(lk, "age") and not info.Age then
				info.Age = tostring(child.Value)
			end
		end
	end
	return info
end

local function buildText(model)
	local data = tracked[model]
	if not data then
		return ""
	end
	local info = data.Info
	local lines = {}
	local nameLine = info.Name or "宠物"
	if info.Rarity then
		nameLine = nameLine .. " [" .. info.Rarity .. "]"
	end
	table.insert(lines, nameLine)
	if info.Level then
		table.insert(lines, "等级: " .. info.Level)
	end
	if info.Age then
		table.insert(lines, "年龄: " .. info.Age)
	end
	if info.Weight then
		table.insert(lines, "重量: " .. info.Weight .. "kg")
	end
	table.insert(lines, "距离: " .. data.Distance .. "m")
	return table.concat(lines, "\n")
end

local function refreshESP(model)
	local data = tracked[model]
	if not data then
		return
	end
	local info = getPetInfo(model)
	if not info.Name and data.Info.Name then
		info.Name = data.Info.Name
	end
	data.Info = info
	local color = getRarityColor(info.Rarity)
	data.Highlight.FillColor = color
	data.Highlight.OutlineColor = color
	data.Label.TextColor3 = color
	if info.Name and isEnglish(info.Name) and not translatedTexts[info.Name] then
		task.spawn(function()
			local translated = translateText(info.Name)
			if translated and translated ~= info.Name and tracked[model] then
				tracked[model].Info.Name = translated
			end
		end)
	elseif info.Name and translatedTexts[info.Name] then
		data.Info.Name = translatedTexts[info.Name]
	end
end

local function addESP(model)
	if tracked[model] then
		return
	end
	if not model:IsA("Model") then
		return
	end
	local info = getPetInfo(model)
	local color = getRarityColor(info.Rarity)
	local highlight = Instance.new("Highlight")
	highlight.Name = "PetESPHighlight"
	highlight.FillColor = color
	highlight.OutlineColor = color
	highlight.FillTransparency = 0.55
	highlight.OutlineTransparency = 0
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
	highlight.Adornee = model
	highlight.Parent = model
	local billboard = Instance.new("BillboardGui")
	billboard.Name = "PetESPBillboard"
	billboard.Size = UDim2.new(0, 220, 0, 80)
	billboard.StudsOffsetWorldSpace = Vector3.new(0, 3.5, 0)
	billboard.AlwaysOnTop = true
	billboard.Adornee = model
	billboard.Parent = model
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.TextColor3 = color
	label.TextStrokeTransparency = 0.3
	label.Font = Enum.Font.GothamBold
	label.TextSize = 14
	label.Text = ""
	label.Parent = billboard
	tracked[model] = {
		Info = info,
		Highlight = highlight,
		Billboard = billboard,
		Label = label,
		Distance = "?"
	}
	if info.Name and isEnglish(info.Name) then
		task.spawn(function()
			local translated = translateText(info.Name)
			if translated and translated ~= info.Name and tracked[model] then
				tracked[model].Info.Name = translated
			end
		end)
	end
	if not info.Name then
		task.delay(1, function()
			if tracked[model] then
				refreshESP(model)
			end
		end)
	end
end

local function removeESP(model)
	local data = tracked[model]
	if not data then
		return
	end
	if data.Highlight then
		data.Highlight:Destroy()
	end
	if data.Billboard then
		data.Billboard:Destroy()
	end
	tracked[model] = nil
end

local function isPetModel(obj)
	return obj:IsA("Model") and obj:FindFirstChild("HumanoidRootPart") ~= nil
end

local function onFolderChild(obj)
	if isPetModel(obj) then
		addESP(obj)
	elseif obj:IsA("Folder") then
		watchFolder(obj)
	end
end

function watchFolder(folder)
	if watchedFolders[folder] then
		return
	end
	watchedFolders[folder] = true
	for _, child in ipairs(folder:GetChildren()) do
		onFolderChild(child)
	end
	table.insert(connections, folder.ChildAdded:Connect(onFolderChild))
	table.insert(connections, folder.ChildRemoved:Connect(function(obj)
		if tracked[obj] then
			removeESP(obj)
		end
	end))
end

local function scanContainers()
	for _, obj in ipairs(Workspace:GetDescendants()) do
		if obj:IsA("Folder") and string.find(string.lower(obj.Name), "pets") then
			watchFolder(obj)
		end
	end
end

local function hookInfoFolder()
	local folder = getInfoFolder()
	if not folder then
		return
	end
	folder.ChildAdded:Connect(function(bb)
		if not espEnabled then
			return
		end
		task.delay(0.5, function()
			for model in pairs(tracked) do
				if model.Name == bb.Name then
					refreshESP(model)
					break
				end
			end
		end)
	end)
end

local function startHeartbeat()
	if heartbeatConn then
		return
	end
	heartbeatConn = RunService.Heartbeat:Connect(function(dt)
		heartbeatAcc = heartbeatAcc + dt
		if heartbeatAcc < 0.15 then
			return
		end
		heartbeatAcc = 0
		local char = LocalPlayer.Character
		local hrp = char and char:FindFirstChild("HumanoidRootPart")
		if not hrp then
			return
		end
		local myPos = hrp.Position
		for model, data in pairs(tracked) do
			if model.Parent then
				local ok, pivot = pcall(function()
					return model:GetPivot()
				end)
				if ok and pivot then
					data.Distance = tostring(math.floor((pivot.Position - myPos).Magnitude))
					data.Label.Text = buildText(model)
				end
			else
				removeESP(model)
			end
		end
	end)
end

local function stopAll()
	if heartbeatConn then
		heartbeatConn:Disconnect()
		heartbeatConn = nil
	end
	for _, conn in ipairs(connections) do
		conn:Disconnect()
	end
	table.clear(connections)
	table.clear(watchedFolders)
	for model in pairs(tracked) do
		removeESP(model)
	end
end

Workspace.DescendantAdded:Connect(function(obj)
	if espEnabled and obj:IsA("Folder") and string.find(string.lower(obj.Name), "pets") then
		watchFolder(obj)
	end
end)

hookInfoFolder()

PlayerGui.ChildAdded:Connect(function(obj)
	if obj.Name == "PetInfoBillboardUIs" then
		hookInfoFolder()
	end
end)

toggleBtn.MouseButton1Click:Connect(function()
	espEnabled = not espEnabled
	if espEnabled then
		toggleBtn.Text = "宠物透视: 开"
		toggleBtn.TextColor3 = Color3.fromRGB(80, 255, 80)
		scanContainers()
		startHeartbeat()
	else
		toggleBtn.Text = "宠物透视: 关"
		toggleBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
		stopAll()
	end
end)
